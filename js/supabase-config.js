/**
 * ATHAR | أثر — Supabase connection config.
 *
 * Fill these in from your Supabase project (Project Settings → API) and
 * see supabase/README.md for the one-time setup steps. The anon/public key
 * is safe to ship in frontend code — every table is protected by Row Level
 * Security policies defined in supabase/schema.sql. NEVER put the
 * service_role key here or anywhere in this repo.
 */
window.ATHAR_SUPABASE_URL = '';
window.ATHAR_SUPABASE_ANON_KEY = '';
