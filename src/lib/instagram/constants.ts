// Short-lived cookie used to carry the OAuth CSRF "state" value between the
// /connect redirect and the /callback that Instagram sends the admin back to.
export const IG_OAUTH_STATE_COOKIE = "ig_oauth_state";
